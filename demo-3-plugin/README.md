# Instruction

```
go run backend/main.go
../traefik --configFile=gateway/traefik.yaml

curl --verbose localhost:8080/api/v3/hello
curl --verbose localhost/api/v3/hello
curl --verbose localhost/api/v1/hello
```
